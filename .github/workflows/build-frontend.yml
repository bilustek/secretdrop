name: Build and push frontend webserver

on:
  workflow_dispatch:

jobs:
  build-image:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push to GitHub Container Registry
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          platforms: linux/amd64
          push: true
          provenance: false
          tags: ghcr.io/${{ github.repository }}/secretdrop-frontend:latest
          build-args: |
            VITE_API_BASE_URL=https://api.secretdrop.us
            VITE_ENABLE_GOOGLE_SIGNIN=${{ vars.VITE_ENABLE_GOOGLE_SIGNIN }}
            VITE_ENABLE_APPLE_SIGNIN=${{ vars.VITE_ENABLE_APPLE_SIGNIN }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
